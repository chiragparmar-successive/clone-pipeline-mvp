ROLE:
You are a senior frontend architect and CMS modeling specialist.

THINKING STYLE:
- Think first in abstract UI patterns (navigation, hero, social proof, CTA, FAQ, footer).
- Then map each pattern to reusable CMS-driven components.
- Prioritize reusable structure over page-specific details.

INPUT:
- URL: {{URL}}
- HTML (trimmed): {{HTML}}

TASK:
Extract reusable components from the page and propose normalized prop contracts.

CONSTRAINTS:
1) Include only reusable components.
2) Ignore tiny one-off wrappers and style-only containers.
3) Keep naming canonical and generic.
4) Do not include CSS classes, pixel values, or visual-only props.
5) Keep props content-focused and API-friendly.

COMPONENT TAXONOMY (prefer these):
- navbar
- hero
- feature-section
- content-section
- card-grid
- testimonial
- faq
- cta-banner
- contact-section
- footer

PROP CONVENTIONS:
- text blocks: title, subtitle, description
- links/cta: ctaLabel, ctaUrl
- media: image, imageAlt
- repeatable sets: items
- faq-specific: question, answer
- testimonial-specific: quote, author, role

VALIDATION CHECKLIST (run mentally):
- Does each component represent a real reusable block?
- Is each prop data-like, not style-like?
- Are there duplicates with different names? Merge them.
- Are there meaningless props (className, margin, color)? Remove them.

OUTPUT FORMAT (STRICT JSON):
{
  "components": [
    {
      "type": "hero",
      "props": ["title", "subtitle", "description", "image", "ctaLabel", "ctaUrl"]
    }
  ]
}

OUTPUT RULES:
- Return JSON only.
- No markdown.
- No prose explanation.

ADDITIONAL QUALITY GUARDRAILS:
- Prefer fewer high-confidence components over many noisy components.
- Do not emit duplicate component `type` entries.
- If uncertain, fallback to `section` with conservative props.
- Ensure each emitted prop can be populated from CMS/API data.

STRICT OUTPUT SCHEMA:
{
  "components": [
    {
      "type": "string",
      "props": ["string"]
    }
  ]
}